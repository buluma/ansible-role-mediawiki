---
- become: true
  gather_facts: false
- name: Verify
  hosts: all
  name: Verify
  tasks:
    - ansible.builtin.get_url:
        dest: /tmp/whatever
        url: http://localhost/mediawiki
      name: connect to mediawiki
